{
  "schemaVersion": 1,
  "id": "sdlc",
  "capability": "data-migration",
  "mode": "default",
  "instructions": "MODULE.md",
  "trigger": "A change transforms, backfills, moves, reinterprets, or deletes durable production data; changes a production schema in a way that requires old and new application or schema versions to coexist; or introduces a migration whose failure could leave persistent state partial or invalid.",
  "exitSignal": "The expand, backfill, switch, and contract sequence is explicit; current production-like rehearsal proves integrity, idempotency or resumability, mixed-version safety, and failure handling; rollback is tested where possible, otherwise a tested mitigation and irreversibility statement are present.",
  "evidence": [
    "A migration inventory identifies source and target representations, volume and shape assumptions, invariants, ownership, retention, and destructive or irreversible steps.",
    "The sequence separates compatible expand, backfill, verification, consumer switch, and destructive contract stages with gates and prerequisites.",
    "Backfill evidence covers applicable batching, checkpoints, idempotency, resume, live writes, error quarantine, and bounded resource use.",
    "Integrity evidence reconciles counts and domain invariants, samples transformed records, detects partial state, and defines acceptance thresholds.",
    "A production-like rehearsal records duration, load, failures, mixed-version compatibility, and post-migration validation.",
    "Rollback evidence restores code, schema, and data where reversible; otherwise a tested mitigation and irreversibility statement are explicit."
  ],
  "compatibleSdlc": ">=1.0.0 <2.0.0"
}
